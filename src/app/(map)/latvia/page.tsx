import {
  LATVIA_DESCRIPTION,
  LATVIA_TITLE,
  createPageMetadata,
} from "@/lib/site-metadata";

export const metadata = createPageMetadata(
  LATVIA_TITLE,
  LATVIA_DESCRIPTION,
  "/latvia",
);

export default function LatviaPage() {
  return null;
}
