import {
  LITHUANIA_DESCRIPTION,
  LITHUANIA_TITLE,
  createPageMetadata,
} from "@/lib/site-metadata";

export const metadata = createPageMetadata(
  LITHUANIA_TITLE,
  LITHUANIA_DESCRIPTION,
  "/lithuania",
);

export default function LithuaniaPage() {
  return null;
}
