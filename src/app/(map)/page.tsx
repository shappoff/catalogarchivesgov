import {
  BELARUS_DESCRIPTION,
  BELARUS_TITLE,
  createPageMetadata,
} from "@/lib/site-metadata";

export const metadata = createPageMetadata(BELARUS_TITLE, BELARUS_DESCRIPTION, "/");

export default function Home() {
  return null;
}
