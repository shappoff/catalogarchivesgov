import {
  POLAND_DESCRIPTION,
  POLAND_TITLE,
  createPageMetadata,
} from "@/lib/site-metadata";

export const metadata = createPageMetadata(
  POLAND_TITLE,
  POLAND_DESCRIPTION,
  "/poland",
);

export default function PolandPage() {
  return null;
}
