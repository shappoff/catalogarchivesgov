import {
  PSKOV_DESCRIPTION,
  PSKOV_TITLE,
  createPageMetadata,
} from "@/lib/site-metadata";

export const metadata = createPageMetadata(
  PSKOV_TITLE,
  PSKOV_DESCRIPTION,
  "/pskov",
);

export default function PskovPage() {
  return null;
}
