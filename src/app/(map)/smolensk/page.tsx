import {
  SMOLENSK_DESCRIPTION,
  SMOLENSK_TITLE,
  createPageMetadata,
} from "@/lib/site-metadata";

export const metadata = createPageMetadata(
  SMOLENSK_TITLE,
  SMOLENSK_DESCRIPTION,
  "/smolensk",
);

export default function SmolenskPage() {
  return null;
}
