import {
  UKRAINE_DESCRIPTION,
  UKRAINE_TITLE,
  createPageMetadata,
} from "@/lib/site-metadata";

export const metadata = createPageMetadata(
  UKRAINE_TITLE,
  UKRAINE_DESCRIPTION,
  "/ukraine",
);

export default function UkrainePage() {
  return null;
}
